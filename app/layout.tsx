import './globals.css';
import type { Metadata } from 'next';
import { Inter } from 'next/font/google';

const inter = Inter({ subsets: ['latin'], variable: '--font-inter' });

export const metadata: Metadata = {
  title: 'AI Wall Visualizer - Transform Your Space',
  description: 'Upload photos of your walls and visualize how different designs, colors, and materials will look using AI-powered technology.',
  keywords: 'wall design, interior design, AI visualization, home decoration, wall panels, wallpaper',
};

export default function RootLayout({
  children,
}: {
  children: React.ReactNode;
}) {
  return (
    <html lang="en">
      <body className={`${inter.variable} font-sans antialiased`}>
        {children}
      </body>
    </html>
  );
}